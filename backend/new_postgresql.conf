# -----------------------------
# PostgreSQL configuration file
# -----------------------------

# This file is created dynamically for debugging purposes.

# Connection Settings
listen_addresses = '*'
port = 5432

# Logging
log_connections = on
log_disconnections = on
log_duration = on

# Authentication
password_encryption = scram-sha-256

# Resource Usage
shared_buffers = 128MB
max_connections = 100 