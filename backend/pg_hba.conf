# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections (from within the container)
local   all             all                                     trust

# Host connections (from your machine)
host    all             erpuser         0.0.0.0/0               scram-sha-256

# Allow connections from any IP if needed (use with caution, for debugging only)
# host    all             all             0.0.0.0/0               scram-sha-256 